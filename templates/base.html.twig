<!DOCTYPE html>
<html lang="{{ app.request.locale }}" dir="{{ app.request.locale == 'ar' ? 'rtl' : 'ltr' }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% block title %}Aychek...{% endblock %}</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Outfit:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <link rel="stylesheet" href="{{ asset('assets/sanad-ui.css') }}">
  <link rel="stylesheet" href="{{ asset('assets/accessibility.css') }}">

  {% block stylesheets %}{% endblock %}
</head>
<body class="theme-light" id="appBody">
  <a href="#main-content" class="skip-link">Passer au contenu haut</a>
  {% if block('hide_nav') is not defined %}
  <header class="topbar-glass animate-down">
    <div class="container topbar-inner">
      <div style="display: flex; align-items: center; gap: 40px;">
        <a class="brand" href="{{ path('app_home') }}">
          <span class="brand-badge"><i class="fa-solid fa-hand-holding-heart"></i></span>
          <span class="brand-name">Aychek...</span>
        </a>

        <nav class="nav-structured">
          <a class="nav-structured-link {{ app.request.attributes.get('_route') == 'case_entity_consult' ? 'active' : '' }}" href="{{ path('case_entity_consult') }}">
            <i class="fa-solid fa-compass"></i> {{ 'nav.explorer'|trans }}
          </a>
          <a class="nav-structured-link {{ app.request.attributes.get('_route') starts with 'app_event' ? 'active' : '' }}" href="{{ path('app_event_index') }}">
            <i class="fa-solid fa-calendar-star"></i> {{ 'nav.events'|trans }}
          </a>
          
          {% if is_granted('ROLE_ASSOCIATION') %}
            <div class="nav-group">
              <span class="nav-group-label"><i class="fa-solid fa-building-ngo"></i> {{ 'nav.espace_pro'|trans }}</span>
              <div class="nav-group-content">
                <a href="{{ path('association_dashboard') }}"><i class="fa-solid fa-gauge-high"></i> {{ 'nav.dashboard'|trans }}</a>
                <a href="{{ path('case_entity_index') }}"><i class="fa-solid fa-plus"></i> {{ 'nav.my_calls'|trans }}</a>
                <a href="{{ path('app_event_index') }}"><i class="fa-solid fa-calendar-days"></i> {{ 'nav.events'|trans }}</a>
                <a href="{{ path('category_index') }}"><i class="fa-solid fa-tags"></i> {{ 'nav.themes'|trans }}</a>
              </div>
            </div>
          {% endif %}

          {% if is_granted('ROLE_ADMIN') %}
              <a class="nav-structured-link admin-link" href="{{ path('admin_dashboard') }}">
                <i class="fa-solid fa-shield-halved"></i> {{ 'nav.admin'|trans }}
              </a>
          {% endif %}
        </nav>
      </div>

        <div class="actions">
        <div class="action-group ghost-pill">
          <select id="langSelect" class="lang-minimal" onchange="window.location.href='/change-locale/'+this.value">
            <option value="fr" {{ app.request.locale == 'fr' ? 'selected' : '' }}>FR</option>
            <option value="en" {{ app.request.locale == 'en' ? 'selected' : '' }}>EN</option>
            <option value="ar" {{ app.request.locale == 'ar' ? 'selected' : '' }}>AR</option>
          </select>
          <button class="theme-minimal" id="themeToggle" type="button" title="Mode Sombre">
            <i class="fa-regular fa-moon"></i>
          </button>
        </div>

        {% if app.user %}
          <div class="user-pill" style="padding: 4px 4px 4px 6px;">
            <a href="{{ path('app_profile_edit') }}" style="display: flex; align-items: center; gap: 10px; text-decoration: none;">
                <div style="width: 32px; height: 32px; border-radius: 50%; overflow: hidden; background: var(--grad-premium); flex-shrink: 0; display: grid; place-items: center; color: white; font-weight: 800; font-size: 0.8rem;">
                    {% if app.user.avatar %}
                        <img src="{{ asset('uploads/avatars/' ~ app.user.avatar) }}" alt="" style="width: 100%; height: 100%; object-fit: cover;">
                    {% else %}
                        {{ app.user.fullName|slice(0, 1)|upper }}
                    {% endif %}
                </div>
                <div class="user-info">
                  <span class="u-name">{{ app.user.fullName }}</span>
                  <span class="u-role">{% if is_granted('ROLE_ADMIN') %}AD{% elseif is_granted('ROLE_ASSOCIATION') %}AS{% else %}DO{% endif %}</span>
                </div>
            </a>
            <a href="{{ path('app_logout') }}" class="logout-btn" title="{{ 'nav.logout'|trans }}"><i class="fa-solid fa-power-off"></i></a>
          </div>
        {% else %}
          <div style="display: flex; gap: 8px;">
            <a href="{{ path('app_login') }}" class="btn btn-sm btn-ghost" style="border-radius: var(--radius-full);">{{ 'nav.connexion'|trans }}</a>
            <a href="{{ path('app_register') }}" class="btn btn-primary btn-sm" style="border-radius: var(--radius-full); box-shadow: var(--shadow-md);">{{ 'nav.register'|trans }}</a>
          </div>
        {% endif %}
      </div>
    </div>
  </header>
  {% endif %}

  {% if block('hide_nav') is not defined %}
  <div class="hero-marquee">
    <div class="hero-marquee-track">
      <span data-i18n="mq_1">✨ Ensemble, changeons des vies • Sanad 2026</span>
      <span data-i18n="mq_2">🧡 Soutien local • Impact immédiat</span>
      <span data-i18n="mq_1">✨ Ensemble, changeons des vies • Sanad 2026</span>
      <span data-i18n="mq_2">🧡 Soutien local • Impact immédiat</span>
      <span data-i18n="mq_1">✨ Ensemble, changeons des vies • Sanad 2026</span>
      <span data-i18n="mq_2">🧡 Soutien local • Impact immédiat</span>
    </div>
  </div>
  {% endif %}

  <main id="main-content" class="page-content" style="padding-top: 1rem; min-height: 80vh; position: relative;">
    {# Floating Notifications #}
    <div style="position: fixed; top: 90px; right: 24px; z-index: 2000; display: flex; flex-direction: column; gap: 12px; pointer-events: none;">
      {% for label, messages in app.flashes %}
        {% for message in messages %}
          <div class="glass-panel animate-up" style="pointer-events: auto; padding: 12px 24px; min-width: 300px; display: flex; align-items: center; gap: 16px; border-left: 4px solid {{ label == 'success' ? 'var(--brand)' : '#ef4444' }}; box-shadow: var(--shadow-xl); animation: slideUp 0.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;">
            <i class="fa-solid {{ label == 'success' ? 'fa-circle-check' : 'fa-circle-exclamation' }}" style="color: {{ label == 'success' ? 'var(--brand)' : '#ef4444' }}; font-size: 1.25rem;"></i>
            <div style="font-weight: 700; font-size: 0.9rem;">{{ message }}</div>
          </div>
        {% endfor %}
      {% endfor %}
    </div>

    {% block body %}{% endblock %}
  </main>

  <footer style="background: var(--bg-subtle); padding: var(--space-xl) 0; border-top: 1px solid var(--border); margin-top: var(--space-2xl);">
    <div class="container">
      <div style="display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 4rem; margin-bottom: 4rem;">
        <div>
          <a class="brand" href="{{ path('case_entity_consult') }}" style="margin-bottom: 1.5rem;">
            <span class="brand-badge"><i class="fa-solid fa-hand-holding-heart"></i></span>
            <span class="brand-name">Sanad</span>
          </a>
          <p class="muted" style="font-size: 0.9375rem; margin-top: 1.5rem; line-height: 1.8;">Sanad est la passerelle de confiance entre la générosité des tunisiens et les besoins urgents du terrain. 100% transparence, 100% impact.</p>
          <div style="display: flex; gap: 16px; margin-top: 2rem;">
            <a href="#" class="btn btn-sm" style="width: 40px; height: 40px; padding: 0; border-radius: 50%;"><i class="fa-brands fa-facebook-f"></i></a>
            <a href="#" class="btn btn-sm" style="width: 40px; height: 40px; padding: 0; border-radius: 50%;"><i class="fa-brands fa-instagram"></i></a>
            <a href="#" class="btn btn-sm" style="width: 40px; height: 40px; padding: 0; border-radius: 50%;"><i class="fa-brands fa-x-twitter"></i></a>
          </div>
        </div>
        <div>
          <h4 style="font-weight: 800; margin-bottom: 1.5rem; font-size: 0.85rem; text-transform: uppercase; letter-spacing: 0.08em; color: var(--text);">Plateforme</h4>
          <ul style="list-style: none; display: flex; flex-direction: column; gap: 12px; padding: 0;">
            <li><a href="{{ path('case_entity_consult') }}" style="text-decoration: none; color: var(--muted); font-size: 0.9rem; font-weight: 600; transition: var(--t-fast);">Découvrir</a></li>
            <li><a href="{{ path('case_entity_index') }}" style="text-decoration: none; color: var(--muted); font-size: 0.9rem; font-weight: 600;">Associations</a></li>
            <li><a href="#" style="text-decoration: none; color: var(--muted); font-size: 0.9rem; font-weight: 600;">Comment ça marche</a></li>
          </ul>
        </div>
        <div>
          <h4 style="font-weight: 800; margin-bottom: 1.5rem; font-size: 0.85rem; text-transform: uppercase; letter-spacing: 0.08em; color: var(--text);">Légal</h4>
          <ul style="list-style: none; display: flex; flex-direction: column; gap: 12px; padding: 0;">
            <li><a href="#" style="text-decoration: none; color: var(--muted); font-size: 0.9rem; font-weight: 600;">Confidentialité</a></li>
            <li><a href="#" style="text-decoration: none; color: var(--muted); font-size: 0.9rem; font-weight: 600;">Conditions</a></li>
            <li><a href="#" style="text-decoration: none; color: var(--muted); font-size: 0.9rem; font-weight: 600;">Transparence</a></li>
          </ul>
        </div>
        <div>
          <h4 style="font-weight: 800; margin-bottom: 1.5rem; font-size: 0.85rem; text-transform: uppercase; letter-spacing: 0.08em; color: var(--text);">Contact</h4>
          <ul style="list-style: none; display: flex; flex-direction: column; gap: 12px; padding: 0;">
            <li style="color: var(--muted); font-size: 0.9rem; font-weight: 600;"><i class="fa-solid fa-envelope" style="margin-right: 8px; color: var(--brand);"></i> contact@sanad.tn</li>
            <li style="color: var(--muted); font-size: 0.9rem; font-weight: 600;"><i class="fa-solid fa-phone" style="margin-right: 8px; color: var(--brand);"></i> +216 71 000 000</li>
          </ul>
        </div>
      </div>
      <div style="border-top: 1px solid var(--border); padding-top: 2rem; display: flex; justify-content: space-between; align-items: center; font-size: 0.8rem; color: var(--muted); font-weight: 600;">
        <div>&copy; 2026 Sanad Tunisie. Fait avec <i class="fa-solid fa-heart" style="color: #ef4444;"></i> pour un meilleur avenir.</div>
        <div style="display: flex; gap: 24px;">
            <span>Hébergé en Tunisie</span>
            <span>v1.0.0-platinum</span>
        </div>
      </div>
    </div>
  </footer>
  <script src="{{ asset('assets/sanad-ui.js') }}"></script>
  <script src="{{ asset('assets/accessibility.js') }}"></script>

  {# Accessibility Suite Overlay & Sidebar #}
  <div class="acc-overlay" id="accOverlay"></div>
  <aside class="acc-sidebar" id="accSidebar" aria-labelledby="acc-title">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem;">
        <h2 id="acc-title" style="font-weight: 800; font-size: 1.25rem; margin: 0;">Accessibilité</h2>
        <button id="accClose" class="btn btn-sm btn-ghost" aria-label="Fermer le menu d'accessibilité" style="width: 36px; height: 36px; padding: 0; border-radius: 50%;"><i class="fa-solid fa-xmark"></i></button>
    </div>

    <div class="acc-section">
        <div class="acc-section-title"><i class="fa-solid fa-text-height"></i> Taille du texte</div>
        <div class="acc-grid">
            <button class="acc-btn" data-acc-type="fontSize" data-acc-value="">Normal</button>
            <button class="acc-btn" data-acc-type="fontSize" data-acc-value="font-size-md">Moyen</button>
            <button class="acc-btn" data-acc-type="fontSize" data-acc-value="font-size-lg">Grand</button>
            <button class="acc-btn" data-acc-type="fontSize" data-acc-value="font-size-xl">Trés Grand</button>
        </div>
    </div>

    <div class="acc-section">
        <div class="acc-section-title"><i class="fa-solid fa-eye"></i> Daltonisme</div>
        <div class="acc-grid">
            <button class="acc-btn" data-acc-type="filter" data-acc-value="acc-protanopia">Protanopie</button>
            <button class="acc-btn" data-acc-type="filter" data-acc-value="acc-deuteranopia">Deutéranopie</button>
            <button class="acc-btn" data-acc-type="filter" data-acc-value="acc-tritanopia">Tritanopie</button>
            <button class="acc-btn" data-acc-type="filter" data-acc-value="acc-grayscale">Grisaille</button>
        </div>
    </div>

    <div class="acc-section">
        <div class="acc-section-title"><i class="fa-solid fa-circle-half-stroke"></i> Affichage</div>
        <div class="acc-grid">
            <button class="acc-btn" data-acc-type="highContrast">Haut Contraste</button>
            <button class="acc-btn" data-acc-type="bigCursor">Grand Curseur</button>
            <button class="acc-btn" data-acc-type="keyboardNav">Navigation Clavier</button>
            <button class="acc-btn" data-acc-type="readAloud"><i class="fa-solid fa-volume-high"></i> Lecteur d'écran</button>
        </div>
    </div>

    <div style="margin-top: 3rem; border-top: 1px solid var(--border); padding-top: 1.5rem;">
        <button id="accReset" class="btn btn-ghost" style="width: 100%; border-radius: var(--radius-md);"><i class="fa-solid fa-rotate-left"></i> Réinitialiser</button>
    </div>
  </aside>

  {# Accessibility Trigger Button #}
  <button class="acc-trigger" id="accTrigger" title="Options d'accessibilité" aria-label="Ouvrir les options d'accessibilité">
    <i class="fa-solid fa-universal-access"></i>
  </button>
  {% block javascripts %}{% endblock %}
</body>
</html>
